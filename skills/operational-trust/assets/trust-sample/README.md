# Trust Sample Service

A small example service used as a fixture for the operational-trust skill scanner.

## Layout

- `status/config.json` — deployment configuration for the status page.
- `src/webhooks.py` — the outbound delivery worker.

## Notes

The fixture intentionally lacks some operational material so the scanner has
gaps to report when it is run against this directory.
