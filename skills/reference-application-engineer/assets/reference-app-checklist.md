# Reference Application Coverage Report

Fill one report per reference application tree. Every concern requires an evidence file and what it demonstrates; every claim carries an evidence label (Observed / CI-observed / Estimated).

## Variant

- Variant: `_______` (minimal / production / multi-tenant / event-driven / high-throughput / serverless / Kubernetes)
- Audience and lesson: `_______`
- Selection rationale: `_______`

## Nine mandatory concerns

| Concern | Evidence file | What it demonstrates |
|---|---|---|
| auth | `_______` | `_______` |
| config | `_______` | `_______` |
| errors | `_______` | `_______` |
| retries | `_______` | `_______` |
| observability | `_______` | `_______` |
| tests | `_______` | `_______` |
| deployment | `_______` | `_______` |
| shutdown | `_______` | `_______` |
| security | `_______` | `_______` |

Checker output: attach `scripts/check_reference_app.py <tree>` output; exit code `___`.

## Run path

- Commands (clean clone to verified outcome): `_______`
- Expected output: `_______`

## Verification

| Check | Result | Evidence |
|---|---|---|
| tests pass from a clean clone | `_______` | `_______` |
| app runs and the documented outcome is produced | `_______` | `_______` |
| checker exits 0 | `_______` | `_______` |
| timing with evidence label (Observed / CI-observed / Estimated) | `_______` | `_______` |

## Known gaps

- `_______` (severity: `_______`)
